## What was wrong

<!-- The behaviour, not the change. If a test found it, say so — that is the
     useful part for whoever reads this next. -->

## Why this fix

<!-- Especially if you considered another one and rejected it. -->

## Checks

- [ ] `make lint` and `make race` pass
- [ ] A test fails without this change
- [ ] No value is printed in a plan, `-json`, a saved plan, or an error
- [ ] Anything upkeep cannot do is a `MANUAL` action with a `why`, not silence
